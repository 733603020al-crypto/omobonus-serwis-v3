# Быстрое решение проблемы 404 на Vercel

## ✅ Проверка структуры проекта (выполнено локально)

- ✅ Главная страница: `src/app/page.tsx` — существует
- ✅ Layout: `src/app/layout.tsx` — существует  
- ✅ Нет ошибок компиляции
- ✅ Файл `vercel.json` удалён (не нужен для Next.js)

## 🔧 Что нужно проверить на Vercel (5 минут)

### Шаг 1: Root Directory
1. Откройте проект на Vercel: https://vercel.com/dashboard
2. Перейдите: **Settings → General**
3. Найдите раздел **"Root Directory"**
4. Убедитесь, что поле **ПУСТОЕ** (или содержит `./`)
5. ❌ Если там указано `src/` или другая папка — удалите и сохраните

### Шаг 2: Framework Preset  
1. В тех же настройках найдите **"Framework Preset"**
2. Должно быть: **Next.js**
3. Если не так — выберите "Next.js" и сохраните

### Шаг 3: Проверка логов
1. Перейдите: **Deployments**
2. Откройте последний деплой (самый верхний)
3. Нажмите **"View Build Logs"**
4. Проверьте:
   - ✅ Сборка завершилась успешно (BUILD SUCCESSFUL)
   - ❌ Если есть ошибки — скопируйте их для анализа

### Шаг 4: Пересборка
1. В том же деплое нажмите кнопку **"Redeploy"** (три точки → Redeploy)
2. Или просто сделайте новый коммит:
   ```bash
   git commit --allow-empty -m "Trigger redeploy"
   git push
   ```

### Шаг 5: Проверка переменных окружения
1. Перейдите: **Settings → Environment Variables**
2. Убедитесь, что добавлены (если используются):
   - `RESEND_API_KEY`
   - `RESEND_FROM_EMAIL`
   - `RESEND_TO_EMAIL`

## 📋 Частые причины 404

1. **Root Directory указан неправильно** — должна быть пустой или `./`
2. **Ошибки сборки** — проверьте логи
3. **Проект не пересобран** — сделайте Redeploy
4. **Неправильная ветка** — убедитесь, что деплой идёт с `main`/`master`

## ✅ После исправлений

Сайт должен открываться:
- Главная: https://omobonus-serwis-v3.vercel.app/
- Услуги: https://omobonus-serwis-v3.vercel.app/uslugi/[slug]

## 🆘 Если не помогло

1. Скопируйте логи сборки
2. Проверьте, что локально работает: `npm run build`
3. Убедитесь, что все файлы в git: `git status`

















