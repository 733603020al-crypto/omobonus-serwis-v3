# 🔍 Диагностика ошибки 404 на Vercel

## ✅ Локальная проверка проекта (выполнена)

### Структура проекта
- ✅ **Главная страница**: `src/app/page.tsx` — существует, правильно экспортирована
- ✅ **Layout**: `src/app/layout.tsx` — существует, правильно экспортирован
- ✅ **Корневой маршрут**: `/` автоматически создаётся из `src/app/page.tsx`
- ✅ **Конфигурация Next.js**: `next.config.ts` — присутствует
- ✅ **TypeScript**: `tsconfig.json` — правильно настроен
- ✅ **Package.json**: все скрипты на месте
- ✅ **Нет ошибок компиляции**: проверено линтером

### Файлы для деплоя
```
✅ package.json          - зависимости и скрипты сборки
✅ next.config.ts        - конфигурация Next.js
✅ tsconfig.json         - конфигурация TypeScript
✅ src/app/page.tsx      - главная страница (маршрут /)
✅ src/app/layout.tsx    - корневой layout
✅ src/app/              - структура App Router
✅ public/               - статические файлы
```

### Что проверено
- ✅ Главная страница экспортирует `export default function Home()`
- ✅ Layout экспортирует `export default function RootLayout()`
- ✅ Нет синтаксических ошибок
- ✅ Все импорты корректны
- ✅ Структура соответствует Next.js App Router

---

## 🔧 Возможные причины ошибки 404

### 1. ⚠️ Root Directory настроен неправильно (наиболее вероятно)

**Проблема**: Vercel ищет проект в неправильной директории.

**Решение**:
1. Откройте проект на Vercel: https://vercel.com/dashboard
2. Перейдите: **Settings → General**
3. Найдите раздел **"Root Directory"**
4. **Очистите поле** (оставьте пустым)
5. Нажмите **"Save"**
6. Пересоберите проект (см. раздел "Пересборка")

### 2. Framework Preset не определён

**Проблема**: Vercel не определил, что это Next.js проект.

**Решение**:
1. В тех же настройках найдите **"Framework Preset"**
2. Выберите **"Next.js"**
3. Сохраните настройки

### 3. Ошибки сборки

**Проблема**: Проект не собирается успешно.

**Решение**:
1. Перейдите: **Deployments → [последний деплой]**
2. Откройте **"Build Logs"**
3. Проверьте ошибки компиляции
4. Исправьте ошибки и пересоберите

### 4. Проект не пересобран после изменений

**Проблема**: Старая версия проекта без главной страницы.

**Решение**: Пересоберите проект (см. раздел "Пересборка")

---

## 🚀 Решение проблемы 404

### Шаг 1: Проверка настроек на Vercel

#### A. Root Directory
1. Откройте: https://vercel.com/dashboard
2. Найдите проект: **omobonus-serwis-v3**
3. Перейдите: **Settings → General**
4. Найдите: **"Root Directory"**
5. **Убедитесь, что поле ПУСТОЕ**
   - Если там `src/` или другая папка — очистите
   - Оставьте пустым и нажмите "Save"

#### B. Framework Preset
1. В тех же настройках найдите: **"Framework Preset"**
2. Должно быть: **Next.js**
3. Если не так — выберите из списка и сохраните

#### C. Build & Development Settings
Проверьте:
- **Build Command**: `npm run build` (автоматически)
- **Output Directory**: `.next` (автоматически)
- **Install Command**: `npm install` (автоматически)

### Шаг 2: Проверка логов сборки

1. Перейдите: **Deployments**
2. Откройте **последний деплой** (самый верхний)
3. Нажмите **"View Build Logs"**

**Проверьте:**
- ✅ Сборка завершилась успешно
- ✅ Видны строки типа "Route (app)" с маршрутами
- ✅ Нет ошибок компиляции
- ❌ Если есть ошибки — исправьте их

**Ищите в логах:**
```
Route (app)                              /                              @/app/page
Route (app)                              /uslugi/[slug]                 @/app/uslugi/[slug]/page
```

Если эти строки есть — маршруты видны Vercel.

### Шаг 3: Пересборка проекта

**Вариант A: Через интерфейс Vercel**
1. В разделе **Deployments** откройте последний деплой
2. Нажмите **три точки** (⋮) рядом с деплоем
3. Выберите **"Redeploy"**
4. Дождитесь завершения сборки

**Вариант B: Через git push**
```bash
git commit --allow-empty -m "Fix: Redeploy for 404 fix"
git push
```

### Шаг 4: Проверка после пересборки

После успешной пересборки проверьте:
- https://omobonus-serwis-v3.vercel.app/
- Страница должна открываться без ошибки 404

---

## 📝 Создание vercel.json (если нужно)

Для Next.js обычно не требуется `vercel.json`, но если проблема сохраняется, можно создать минимальный файл:

```json
{
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
```

**Важно**: Сначала попробуйте без `vercel.json` — просто очистите Root Directory.

---

## ✅ Чеклист исправления

- [ ] Root Directory на Vercel очищен (пустое поле)
- [ ] Framework Preset выбран как Next.js
- [ ] Логи сборки проверены (нет ошибок)
- [ ] Проект пересобран (Redeploy)
- [ ] Главная страница открывается по https://omobonus-serwis-v3.vercel.app/

---

## 🎯 Ожидаемый результат

После выполнения всех шагов:
- ✅ Сайт открывается по адресу: https://omobonus-serwis-v3.vercel.app/
- ✅ Главная страница отображается без ошибки 404
- ✅ Все маршруты работают корректно

---

## 🆘 Если проблема сохраняется

1. **Скопируйте логи сборки** из Vercel
2. **Проверьте локальную сборку**: `npm run build`
3. **Убедитесь, что все файлы в git**: `git status`
4. **Проверьте ветку**: убедитесь, что деплой идёт с правильной ветки

---

**Основная причина 404 обычно — неправильный Root Directory на Vercel. Начните с проверки этого параметра!**

















