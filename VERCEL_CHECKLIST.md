# Чеклист для проверки деплоя на Vercel

## ✅ Локальная проверка проекта

### Структура проекта (проверено)
- ✅ Главная страница: `src/app/page.tsx` — существует и правильно экспортирована
- ✅ Layout: `src/app/layout.tsx` — существует и правильно экспортирован
- ✅ Package.json: присутствует с правильными скриптами сборки
- ✅ Next.js конфигурация: `next.config.ts` — существует

### Команды сборки
```bash
# Проверка локальной сборки
npm run build

# Если сборка успешна, проект готов к деплою
```

## 🔧 Настройки на Vercel (проверьте вручную)

### 1. Root Directory
- Перейдите: **Settings → General → Root Directory**
- Должно быть: **пусто** (или `./` если проект в корне репозитория)
- ❌ НЕ должно быть: `src/` или другие подпапки

### 2. Framework Preset
- Перейдите: **Settings → General → Framework Preset**
- Должно быть: **Next.js**
- Vercel обычно определяет автоматически

### 3. Build & Development Settings
- **Build Command**: `npm run build` (автоматически)
- **Output Directory**: `.next` (автоматически)
- **Install Command**: `npm install` (автоматически)
- **Development Command**: `npm run dev` (автоматически)

### 4. Environment Variables
Проверьте наличие переменных окружения:
- `RESEND_API_KEY` (если используется)
- `RESEND_FROM_EMAIL` (если используется)
- `RESEND_TO_EMAIL` (если используется)

Перейдите: **Settings → Environment Variables**

## 🔍 Проверка деплоя

### 1. Логи сборки
- Перейдите: **Deployments → [последний деплой]**
- Откройте вкладку **"Build Logs"**
- Проверьте, есть ли ошибки:
  - ❌ Ошибки компиляции TypeScript
  - ❌ Ошибки импорта модулей
  - ❌ Ошибки сборки Next.js
  - ❌ Проблемы с зависимостями

### 2. Функциональные логи
- Откройте вкладку **"Function Logs"**
- Проверьте ошибки во время выполнения

### 3. Проверка маршрутов
После успешного деплоя проверьте:
- ✅ Главная страница: `https://omobonus-serwis-v3.vercel.app/`
- ✅ Страницы услуг: `https://omobonus-serwis-v3.vercel.app/uslugi/[slug]`

## 🚀 Действия для исправления

### Если ошибка 404:

1. **Проверьте Root Directory**
   - Убедитесь, что Root Directory пусто или указывает на корень проекта

2. **Проверьте логи сборки**
   - Откройте последний деплой
   - Посмотрите, успешно ли завершилась сборка

3. **Пересоберите проект**
   - В настройках деплоя нажмите **"Redeploy"**
   - Или сделайте новый коммит и пуш

4. **Проверьте настройки репозитория**
   - Убедитесь, что Vercel подключен к правильной ветке
   - Обычно это `main` или `master`

5. **Проверьте файл .gitignore**
   - Убедитесь, что `.next` и `node_modules` в .gitignore
   - Но файлы `src/`, `package.json`, `next.config.ts` должны быть в репозитории

## 📝 Важные замечания

- Файл `vercel.json` **НЕ требуется** для Next.js — Vercel определяет проект автоматически
- Структура `src/app/` правильная для Next.js App Router
- Главная страница должна быть доступна по маршруту `/`

## 🔗 Полезные ссылки

- [Vercel Documentation - Next.js](https://vercel.com/docs/frameworks/nextjs)
- [Next.js Deployment Documentation](https://nextjs.org/docs/deployment)

## ❓ Если проблема сохраняется

1. Скопируйте логи сборки из Vercel
2. Проверьте, что проект собирается локально (`npm run build`)
3. Убедитесь, что все файлы закоммичены и запушены в репозиторий

















