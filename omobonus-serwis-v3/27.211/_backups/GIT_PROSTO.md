# 🎯 Git — простыми словами

## ✅ У вас уже всё настроено!

- **Git установлен:** версия 2.52.0
- **Ваше имя:** Andrii
- **Email:** 733603020a@gmail.com
- **Репозиторий создан:** проект уже подключен к Git

---

## 📖 Что такое Git? (очень просто)

**Git** — это как "машина времени" для вашего кода.

### Простая аналогия:
Представьте, что вы пишете документ в Word. Git — это как кнопка "Сохранить", но с возможностью:
- ✅ Вернуться к любой старой версии
- ✅ Посмотреть, что изменилось
- ✅ Не потерять файлы, если что-то сломается

---

## 🚀 Три главные команды (для начала)

### 1️⃣ **Посмотреть, что изменилось**
```bash
git status
```
**Что делает:** Показывает, какие файлы вы изменили, но ещё не сохранили.

**Когда использовать:** В начале работы или когда хотите проверить изменения.

---

### 2️⃣ **Подготовить файлы к сохранению**
```bash
git add .
```
**Что делает:** Говорит Git: "Эти файлы я хочу сохранить".

**Когда использовать:** После того, как вы внесли изменения и хотите их сохранить.

**Варианты:**
- `git add .` — добавить ВСЕ измененные файлы
- `git add src/components/header.tsx` — добавить только один файл

---

### 3️⃣ **Сохранить версию (сделать "снимок")**
```bash
git commit -m "Описание того, что вы сделали"
```
**Что делает:** Сохраняет текущее состояние проекта с описанием изменений.

**Когда использовать:** После `git add`, когда готовы сохранить версию.

**Примеры описаний:**
```bash
git commit -m "Исправил ошибку в services.tsx"
git commit -m "Добавил новые стили для карточек"
git commit -m "Восстановил файлы из бэкапа"
git commit -m "Обновил инструкции"
```

**Важно:** Описание должно быть понятным, чтобы потом вы могли вспомнить, что изменилось.

---

## 📋 Обычный рабочий процесс

### Каждый день работы:

**1. Начало работы:**
```bash
git status
```
Посмотреть, что изменилось с последнего раза.

---

**2. После внесения изменений:**

Например, вы изменили файл `src/components/header.tsx`:

```bash
# Шаг 1: Подготовить файлы
git add .

# Шаг 2: Сохранить с описанием
git commit -m "Обновил навигацию в header"
```

**Готово!** Изменения сохранены.

---

**3. Если что-то сломалось:**

```bash
# Посмотреть историю сохранений
git log

# Вернуть все файлы к последнему сохранению
git restore .
```

---

## 📚 Полезные команды

### Посмотреть историю сохранений:
```bash
git log
```
Показывает все ваши сохранения с датами и описаниями.

**Как выйти:** Нажмите `q` (буква Q)

---

### Посмотреть, что именно изменилось:
```bash
git diff
```
Показывает построчно, что вы изменили в файлах.

---

### Отменить изменения в одном файле:
```bash
git restore src/components/header.tsx
```
Вернет файл к последнему сохраненному состоянию.

---

### Отменить ВСЕ изменения:
```bash
git restore .
```
**Внимание:** Это вернет все файлы к последнему сохранению. Несохраненные изменения пропадут!

---

## 💾 Когда сохранять?

**Сохраняйте версию после:**
- ✅ Исправления ошибки
- ✅ Добавления новой функции
- ✅ Изменения стилей
- ✅ Восстановления файлов
- ✅ В конце рабочего дня

**Правило:** Лучше сохранять чаще, чем реже!

---

## 🔍 Что у вас сейчас?

Сейчас у вас есть **несохраненные изменения** в файлах:
- `_backups/ANALYSIS_workspace-issue_2025-11-24.md`
- `_backups/GIT_SETUP_INSTRUCTIONS.md`
- `_backups/SETUP_AUTOMATIC_BACKUPS.md`
- `_backups/backup-script.ps1`
- `src/app/uslugi/[slug]/page.tsx`
- `src/components/sections/hero.tsx`
- `src/lib/services-data.ts`
- `tailwind.config.ts`

**Чтобы сохранить их:**
```bash
git add .
git commit -m "Добавил инструкции по Git и скрипт бэкапа"
```

---

## ⚠️ Важные моменты

### 1. Git не удаляет файлы автоматически
Все команды безопасны. Git не удалит ваши файлы без вашего явного указания.

### 2. Описания должны быть понятными
```bash
# ❌ Плохо:
git commit -m "изменения"

# ✅ Хорошо:
git commit -m "Исправил ошибку 404 на страницах услуг"
```

### 3. Можно сохранять несколько раз в день
Не бойтесь делать много сохранений. Это нормально и полезно!

---

## 🎓 Пример полного цикла

**Сценарий:** Вы исправили ошибку в `services.tsx`

```bash
# 1. Проверить, что изменилось
git status

# 2. Подготовить файлы
git add src/components/sections/services.tsx

# 3. Сохранить с описанием
git commit -m "Исправил ошибку отображения карточек услуг"

# 4. Проверить, что всё сохранилось
git status
# Должно показать: "nothing to commit, working tree clean"
```

---

## 📞 Если что-то пошло не так

### "Я случайно удалил файл!"
```bash
git restore имя_файла.tsx
```

### "Я хочу вернуться к вчерашней версии"
```bash
git log                    # Найти нужное сохранение
git restore .              # Вернуть все файлы
```

### "Я забыл, что изменил"
```bash
git status                # Показать измененные файлы
git diff                  # Показать, что именно изменилось
```

---

## 🎯 Итого: минимум для работы

**Три команды, которые нужно запомнить:**

1. `git status` — что изменилось?
2. `git add .` — подготовить к сохранению
3. `git commit -m "описание"` — сохранить

**Всё!** Этого достаточно для начала. Остальное можно изучить позже.

---

**Удачи! 🚀**















