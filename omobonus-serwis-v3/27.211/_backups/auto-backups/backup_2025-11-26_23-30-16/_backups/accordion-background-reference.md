# Референсное состояние фона аккордеона

**Дата создания:** 25.11.2025  
**Статус:** ✅ Стабильная версия (референсная база)

## 📋 Описание текущей реализации

### Метод наложения фона
- ✅ Фон применяется **напрямую** через `background-image` с комбинацией `linear-gradient(...) + url(...)`
- ❌ **Без** псевдоэлементов `::before` и `::after`
- ✅ Фон каждого блока **изолирован** от фона страницы

### Параметры затемнения

#### Шапка (`.accordion-header-bg`)
- **Обычное состояние:** 30% черного (`rgba(0, 0, 0, 0.3)`)
- **При hover:** 35% черного (`rgba(0, 0, 0, 0.35)`)

#### Тело таблицы (`[data-slot="accordion-content"].accordion-content-bg[data-state="open"] > div`)
- **Обычное состояние:** 25% черного (`rgba(0, 0, 0, 0.25)`)
- **При hover:** 30% черного (`rgba(0, 0, 0, 0.3)`)

### Фоновое изображение
- **Источник:** `/images/services-background.png`
- **Применение:** Каждый открытый блок аккордеона имеет свой собственный изолированный фон
- **Эффект:** Задняя текстура страницы не просвечивает через содержимое аккордеона

## 🎯 Ключевые характеристики

1. **Изоляция фона:** Каждый блок имеет свою "стену" (services-background.png), а не просвечивание общей текстуры
2. **Визуальное разделение:** Шапка темнее тела (30% vs 25%), что создает четкую границу
3. **Плавные переходы:** Hover-эффекты увеличивают затемнение на 5%
4. **Чистота реализации:** Нет сложных псевдоэлементов, фон применяется напрямую

## 📝 Что НЕ изменяется (сохраняется)

- ✅ Шрифты, размеры, межстрочные интервалы
- ✅ Цвета текста и иконок
- ✅ Отступы и бордеры
- ✅ Тени и прозрачности элементов
- ✅ Структура JSX/TSX

## 🔧 Текущий CSS (из `globals.css`)

```css
/* Фон шапки в открытом состоянии - services-background с затемнением */
.accordion-header-bg {
  background-image: 
    linear-gradient(rgba(0, 0, 0, 0.3), rgba(0, 0, 0, 0.3)),
    url('/images/services-background.png');
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
  transition: background-image 0.3s ease;
}

.accordion-header-bg:hover {
  background-image: 
    linear-gradient(rgba(0, 0, 0, 0.35), rgba(0, 0, 0, 0.35)),
    url('/images/services-background.png');
}

/* Фон тела таблицы в открытом состоянии - services-background с затемнением (светлее шапки) */
[data-slot="accordion-content"].accordion-content-bg[data-state="open"] > div {
  background-image: 
    linear-gradient(rgba(0, 0, 0, 0.25), rgba(0, 0, 0, 0.25)),
    url('/images/services-background.png');
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
  transition: background-image 0.3s ease;
}

[data-slot="accordion-content"].accordion-content-bg[data-state="open"]:hover > div {
  background-image: 
    linear-gradient(rgba(0, 0, 0, 0.3), rgba(0, 0, 0, 0.3)),
    url('/images/services-background.png');
}
```

## 🎨 Визуальный результат

- ✅ Фон плотный, текстура четкая, нет размытия
- ✅ Границы между блоками видны четко
- ✅ Фон каждого блока изолирован (не "слипается" с соседними)
- ✅ В DevTools видно, что фоновое изображение прикреплено к каждому блоку, а не наследуется от страницы

## 📌 Использование как базы

Это состояние можно использовать как **референсную точку** для будущих настроек контраста:
- Например, для увеличения контраста можно изменить затемнение на 65% / 70%
- Метод наложения (прямой `background-image` с `linear-gradient`) остается неизменным
- Изоляция фона сохраняется

## ⚠️ Важно

При любых будущих изменениях фона аккордеона:
1. Сохраняйте метод прямого применения через `background-image`
2. Не возвращайтесь к псевдоэлементам `::before` / `::after`
3. Сохраняйте изоляцию фона от страницы
4. Не изменяйте структуру JSX/TSX, шрифты, цвета текста, отступы

