# 📚 Что такое Git и как его использовать

## 🎯 Что такое Git простыми словами?

**Git** — это система контроля версий. Представьте, что это как "машина времени" для вашего кода:

- ✅ **Сохраняет историю изменений** — вы можете вернуться к любой версии проекта
- ✅ **Защита от потери файлов** — если что-то сломается, можно откатиться назад
- ✅ **Работа в команде** — несколько человек могут работать над проектом одновременно
- ✅ **Бэкапы** — код хранится не только на вашем компьютере, но и в облаке (GitHub)

### Простая аналогия:
Представьте, что вы пишете книгу. Git — это как автоматическое сохранение каждой страницы с возможностью вернуться к любой версии. Если вы случайно удалите главу, Git поможет её восстановить!

---

## 📥 Установка Git

### Шаг 1: Скачать Git
1. Откройте браузер и перейдите на: **https://git-scm.com/download/win**
2. Скачайте установщик для Windows (автоматически определит 64-bit или 32-bit)
3. Запустите скачанный файл (например, `Git-2.43.0-64-bit.exe`)

### Шаг 2: Установка
1. **Нажимайте "Next"** на всех экранах (настройки по умолчанию подходят)
2. **Важно:** Оставьте галочку "Git from the command line and also from 3rd-party software"
3. Завершите установку

### Шаг 3: Проверка
После установки:
1. **Закройте и откройте Cursor заново** (чтобы обновились пути)
2. Или откройте новое окно PowerShell
3. Выполните команду: `git --version`
4. Должно появиться: `git version 2.xx.x` (версия может отличаться)

---

## 🚀 Первая настройка Git

После установки нужно один раз настроить Git (указать ваше имя и email):

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш@email.com"
```

**Пример:**
```bash
git config --global user.name "Andrii"
git config --global user.email "andrii@example.com"
```

---

## 📦 Основные команды Git (для начала)

### 1. **Инициализация репозитория** (один раз в начале проекта)
```bash
git init
```
Создает "хранилище" для вашего проекта в текущей папке.

### 2. **Проверка статуса** (что изменилось?)
```bash
git status
```
Показывает, какие файлы были изменены, добавлены или удалены.

### 3. **Добавление файлов** (подготовка к сохранению)
```bash
git add .
```
Добавляет все измененные файлы. Или можно добавить конкретный файл:
```bash
git add src/components/header.tsx
```

### 4. **Создание "снимка"** (сохранение версии)
```bash
git commit -m "Описание того, что вы изменили"
```
**Примеры:**
```bash
git commit -m "Добавлена секция Hero"
git commit -m "Исправлена ошибка в services.tsx"
git commit -m "Обновлены стили карточек"
```

### 5. **Просмотр истории** (все сохраненные версии)
```bash
git log
```
Показывает все коммиты (сохранения) с датами и описаниями.

### 6. **Откат к предыдущей версии** (если что-то сломалось)
```bash
git checkout -- .
```
Вернет все файлы к последнему сохраненному состоянию.

---

## 🔄 Рабочий процесс (как использовать Git каждый день)

### Обычный день работы:
1. **Начало работы:**
   ```bash
   git status  # Проверить, что все в порядке
   ```

2. **После внесения изменений:**
   ```bash
   git add .                    # Подготовить файлы
   git commit -m "Описание"      # Сохранить версию
   ```

3. **Если что-то сломалось:**
   ```bash
   git log                      # Посмотреть историю
   git checkout -- .            # Откатить изменения
   ```

### Рекомендация:
**Сохраняйте версию после каждого значимого изменения!** Например:
- После добавления новой секции
- После исправления ошибки
- После изменения стилей
- В конце рабочего дня

---

## ☁️ Синхронизация с GitHub (опционально, но рекомендуется)

GitHub — это облачное хранилище для вашего кода. Это как "Google Drive" для программистов.

### Преимущества:
- ✅ Бэкап в облаке (даже если сломается компьютер)
- ✅ Доступ к коду с любого устройства
- ✅ История всех изменений
- ✅ Бесплатно для личных проектов

### Как настроить (после установки Git):

1. **Создать аккаунт на GitHub:**
   - Перейдите на: https://github.com
   - Зарегистрируйтесь (бесплатно)

2. **Создать репозиторий:**
   - Нажмите "New repository"
   - Назовите его (например, `omobonus-serwis-v3`)
   - Выберите "Private" (приватный)
   - Нажмите "Create repository"

3. **Подключить к проекту:**
   ```bash
   git remote add origin https://github.com/ваш-username/omobonus-serwis-v3.git
   git push -u origin main
   ```

4. **Отправлять изменения в облако:**
   ```bash
   git push
   ```

---

## 🛡️ Защита от проблем (как мы использовали бэкапы)

Git решает те же проблемы, что и бэкапы, но автоматически:

| Проблема | Решение с Git |
|----------|---------------|
| Файлы удалились | `git checkout -- .` восстановит их |
| Код сломался | `git log` → найти рабочую версию → `git checkout <версия>` |
| Нужна старая версия | `git log` → посмотреть историю → вернуться к нужной версии |
| Потерялся компьютер | Код в GitHub (облаке) |

---

## 📝 Чеклист для начала работы

- [ ] Установить Git с https://git-scm.com/download/win
- [ ] Настроить имя и email: `git config --global user.name "..."` и `git config --global user.email "..."`
- [ ] Выполнить `git init` в папке проекта
- [ ] Выполнить `git add .` и `git commit -m "Initial commit"`
- [ ] (Опционально) Создать аккаунт на GitHub и подключить репозиторий

---

## ❓ Частые вопросы

**Q: Нужно ли знать все команды Git?**  
A: Нет! Для начала достаточно 5-6 команд (init, status, add, commit, log, checkout).

**Q: Можно ли использовать Git без GitHub?**  
A: Да! Git работает локально на вашем компьютере. GitHub — это опциональное облачное хранилище.

**Q: Что если я сделаю ошибку в команде?**  
A: Git безопасен. Команды не удаляют файлы без предупреждения. Всегда можно откатиться.

**Q: Как часто нужно делать commit?**  
A: После каждого значимого изменения (добавили функцию, исправили ошибку, изменили стили).

---

**После установки Git, я помогу вам настроить его для вашего проекта!** 🚀

