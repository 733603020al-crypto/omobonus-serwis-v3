# 🔄 Настройка автоматических бэкапов

## 📋 Что это дает?

Автоматические бэкапы создают копию вашего проекта каждый день (или по расписанию), чтобы:
- ✅ Защитить от потери файлов
- ✅ Можно было вернуться к предыдущей версии
- ✅ Не нужно помнить о ручном создании бэкапов

---

## 🚀 Быстрая настройка (5 минут)

### Шаг 1: Проверить скрипт бэкапа

Скрипт уже создан: `_backups/backup-script.ps1`

### Шаг 2: Протестировать вручную

1. Откройте PowerShell в папке проекта
2. Выполните:
   ```powershell
   .\_backups\backup-script.ps1
   ```
3. Проверьте, что бэкап создался в `_backups/auto-backups/backup_YYYY-MM-DD_HH-mm-ss/`

### Шаг 3: Настроить автоматический запуск

#### Вариант A: Через Windows Task Scheduler (рекомендуется)

1. **Откройте "Планировщик заданий"** (Task Scheduler):
   - Нажмите `Win + R`
   - Введите: `taskschd.msc`
   - Нажмите Enter

2. **Создайте новое задание:**
   - Нажмите "Создать задачу" (Create Task) справа
   - Вкладка "Общие" (General):
     - Имя: `Omobonus Serwis - Daily Backup`
     - Описание: `Автоматический бэкап проекта Omobonus Serwis`
     - ✅ Запускать с наивысшими правами (Run with highest privileges)

3. **Вкладка "Триггеры" (Triggers):**
   - Нажмите "Создать" (New)
   - Начало задачи: `По расписанию`
   - Параметры: `Ежедневно`
   - Время: `22:00` (или любое удобное время)
   - ✅ Включено
   - Нажмите "OK"

4. **Вкладка "Действия" (Actions):**
   - Нажмите "Создать" (New)
   - Действие: `Запустить программу`
   - Программа: `powershell.exe`
   - Аргументы: `-ExecutionPolicy Bypass -File "C:\Users\Andrii\omobonus-serwis-v3\_backups\backup-script.ps1"`
   - Нажмите "OK"

5. **Вкладка "Условия" (Conditions):**
   - ✅ Запускать задачу только при питании от электросети (если ноутбук)
   - ✅ Пробуждать компьютер для выполнения задачи (опционально)

6. **Вкладка "Параметры" (Settings):**
   - ✅ Разрешить выполнение задачи по требованию
   - ✅ Выполнять задачу немедленно, если пропущен запланированный запуск

7. **Нажмите "OK"** и введите пароль администратора (если потребуется)

#### Вариант B: Через ярлык на рабочем столе

1. Создайте ярлык на рабочем столе
2. Путь к программе: `powershell.exe`
3. Аргументы: `-ExecutionPolicy Bypass -File "C:\Users\Andrii\omobonus-serwis-v3\_backups\backup-script.ps1"`
4. Запускайте вручную, когда нужно

---

## 📁 Где хранятся бэкапы?

Все автоматические бэкапы сохраняются в:
```
C:\Users\Andrii\omobonus-serwis-v3\_backups\auto-backups\
```

Структура:
```
_backups/
  auto-backups/
    backup_2025-11-24_22-00-00/
      src/
      public/
      tailwind.config.ts
      postcss.config.js
      ...
      BACKUP_INFO.txt
    backup_2025-11-25_22-00-00/
      ...
```

---

## ⚙️ Настройки скрипта

Вы можете изменить скрипт `backup-script.ps1`:

### Изменить, что копируется:
Отредактируйте массив `$itemsToBackup`:
```powershell
$itemsToBackup = @(
    "src",
    "public",
    "tailwind.config.ts",
    # Добавьте свои файлы/папки
)
```

### Изменить количество хранимых бэкапов:
В конце скрипта найдите:
```powershell
if ($allBackups.Count -gt 10) {  # Измените 10 на нужное число
```

### Изменить путь к бэкапам:
В начале скрипта:
```powershell
$backupPath = "C:\Users\Andrii\omobonus-serwis-v3\_backups\auto-backups"
```

---

## 🔍 Проверка работы

### Проверить, что бэкапы создаются:

1. Запустите скрипт вручную:
   ```powershell
   .\_backups\backup-script.ps1
   ```

2. Проверьте папку:
   ```powershell
   dir _backups\auto-backups
   ```

3. Проверьте содержимое бэкапа:
   ```powershell
   dir _backups\auto-backups\backup_2025-11-24_22-00-00
   ```

### Проверить задачу в планировщике:

1. Откройте "Планировщик заданий"
2. Найдите задачу "Omobonus Serwis - Daily Backup"
3. Правый клик → "Выполнить" (Run) — для тестирования
4. Правый клик → "История" (History) — для просмотра логов

---

## 🛠️ Восстановление из бэкапа

Если нужно восстановить файлы из бэкапа:

1. Найдите нужный бэкап в `_backups/auto-backups/backup_YYYY-MM-DD_HH-mm-ss/`
2. Скопируйте нужные файлы обратно в проект
3. Или используйте PowerShell:
   ```powershell
   Copy-Item -Path "_backups\auto-backups\backup_2025-11-24_22-00-00\src\*" -Destination "src\" -Recurse -Force
   ```

---

## ⚠️ Важные замечания

1. **Бэкапы хранятся локально** — если сломается диск, бэкапы тоже пропадут
2. **Рекомендуется:** Дополнительно использовать Git + GitHub для облачного хранения
3. **Проверяйте бэкапы:** Периодически проверяйте, что бэкапы создаются корректно

---

## 📊 Статистика бэкапов

Скрипт автоматически:
- ✅ Сохраняет последние 10 бэкапов
- ✅ Удаляет старые бэкапы (чтобы не занимать место)
- ✅ Создает файл `BACKUP_INFO.txt` с информацией о бэкапе

---

**После настройки автоматических бэкапов, ваш проект будет защищен от потери файлов!** 🛡️

